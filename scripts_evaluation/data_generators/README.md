# Data generators

Chỉ chứa code tạo dữ liệu kiểm thử, ground truth hoặc biến thể input:

- `scf/`: operation plan và renderer CMD/PowerShell/smbclient.
- `pcapfs/`: multi-version SMB test, scale workload và loss PCAP.

Dữ liệu sinh tự động không được gọi là natural hold-out trong báo cáo.
