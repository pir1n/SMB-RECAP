def export_files(file_table):
    result = []

    for file_id, f in file_table.files.items():
        result.append({
            "file_id": file_id,
            "path": f.path,
            "size": f.size,
            "chunks": f.chunks,
        })

    return {
        "files": result
    }