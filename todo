1. Trong file pcap_reader.py việc nó đọc sai các fields và đọc thiếu nhiều fields khi so lại với packet được đọc trên wireshark ví dụ như packet 624 fields smb2_is_response nó phải là false chứ không phải true vì trong wireshark nó là read_request mà trong read_request là phải có file_id nhưng nó lại không đọc được 
    ảo hơn nữa là file_id không có nhưng nó map được file_id mapped_file_id tui dùng cho response do lúc trước nó không có file_id trong response

(fixed)

2. Fix response_to filename file_id (fixed)

3. Code parse filesystemtree, metadata và versioning (cần xem lại logic của phần code này) versioning đang lấy timestamp dựa trên timestamp packet chứ không phải timestamp query_info cần code lại metadata. (đã fix nhưng xảy ra bug khác)

4. Giải quyết vấn đề modified time trong output của versioning!!. Thử mapping last_write_time last_change_time từ create_response trước đó của packet CREATE vào write request packet / read  response packet theo File_id

5. Tạo thêm test to to để xác định version chính xác hơn, khai thác vô GetInfo (query_info) để kiếm last_write_time hoặc last_change_time, cải thiện duplicate. File có nhiều file_id thì gom lại, phân version cho file

6. Cần parse thêm các trường cần thiết cho SCF như là :
desired_access
create_disposition
create_options
file_info_class

Viết vào get_smb2_payload()

